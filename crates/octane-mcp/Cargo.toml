[package]
name = "octane-mcp"
description = "MCP client: JSON-RPC lifecycle over stdio and streamable HTTP."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
camino.workspace = true
octane-tools.workspace = true
octane-protocol.workspace = true
async-trait.workspace = true
reqwest.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tokio.workspace = true

[dev-dependencies]
octane-permission.workspace = true
tempfile = "3"
# `net` is not in the workspace feature set: only the HTTP transport's test
# needs a listener, and it needs a real one to prove bytes move.
tokio = { workspace = true, features = ["net"] }

[lints]
workspace = true
